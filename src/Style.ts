import { styled } from '@mui/system';

export const Title = styled('div')({
    fontWeight: 'bold',
    fontSize: 24,
    textAlign: 'center',
});

export const CompleteNumber = styled('div')({
    fontSize: 16,
    marginTop: 16,
});

export const TotalBox = styled('div')({
    margin: 32,
    padding: 16,
    width: 300,
    border: '1px dashed',
});